# Upgrade pip first
```bash
python -m pip install --upgrade pip
```

# Install requirements
```bash
pip install -r requirements.txt
```
# If pytgcalls fails, try:
```bash
pip install py-tgcalls==0.9.0
```
